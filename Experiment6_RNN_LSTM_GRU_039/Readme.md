CS3807 Deep Learning Laboratory – Experiment 6
Compares Vanilla RNN, LSTM, and GRU for sequence learning using the UCI Human Activity Recognition dataset.
Covers BPTT, vanishing gradients, and exploding gradients.
Visualizes temporal patterns in human activity sensor signals.
Evaluates RNN, LSTM, and GRU using accuracy, precision, recall, F1-score, parameter count, training time, and confusion matrices.
Studies the effect of different sequence lengths on classification performance.
Implements video understanding using UCF101 with MobileNetV2 for frame-level feature extraction and LSTM/GRU for temporal modelling.
Includes video training curves, sample frames, and confusion-matrix analysis.
Implements a sequence-to-sequence encoder–decoder reversal task and evaluates token and sequence accuracy.
Includes additional experiments on recurrent units, stacked LSTM, bidirectional LSTM, sequence length, and CNN–LSTM vs CNN–GRU.
Contains the complete implementation, experimental results, figures, tables, LaTeX report, and compiled PDF.
